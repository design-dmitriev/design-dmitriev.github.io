# --- Зависимости ---
node_modules/

# --- Локальные настройки инструментов ---
.claude/settings.local.json

# --- Мусор ОС и редакторов ---
Thumbs.db
desktop.ini
.DS_Store
*.swp
.vscode/
.idea/

# --- Исходники, не относящиеся к сайту ---
# Рабочие выгрузки и фотоархив. Лежат на диске, но в репозиторий не попадают:
# Выгрузка_Баннеры — полный дубль assets/portfolio/Реклама,
# Фотки — нигде на сайте не используются.
/Выгрузка/
/Выгрузка_Баннеры/
/Обложки/
/Фотки/

# --- Медиа сайта: временно вне репозитория ---
# Сейчас это ~75 МБ несжатых PNG и MP3. После перевода в webp и сжатия
# (шаг 3 плана) эти правила снимаются, и оптимизированные ассеты
# добавляются в репозиторий — чтобы тяжёлые версии не осели в истории.
/assets/
/background.mp3
/CV_Starlight.pdf
